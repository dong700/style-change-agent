# 项目打包说明

## 打包前准备

### 1. 清理不必要的文件

打包前建议删除以下内容：

```
# 删除缓存文件
cache/
output/
__pycache__/
*.pyc
.ipynb_checkpoints/

# 删除测试和说明文件（可选保留）
test_*.py
*.md（保留 使用说明.md）
```

### 2. 保留的核心文件

必须保留的文件和目录：

```
style_change_agent/
├── web_app/              # Web 应用（必须）
│   ├── app.py
│   ├── large_text_api.py
│   └── model_config.json
├── large_text_processor/ # 文档处理（必须）
├── templates/            # 网页模板（必须）
├── style_extractor.py    # 风格提取（必须）
├── style_rewriter.py     # 风格改写（必须）
├── requirements.txt      # 依赖清单（必须）
├── requirements_ocr.txt  # OCR 依赖（必须）
├── 使用说明.md           # 使用说明（必须）
└── 启动服务.bat          # 启动脚本（必须）
```

---

## 打包方法

### 方法一：Windows 右键打包

1. 进入项目目录的上一级
2. 右键点击 `style_change_agent` 文件夹
3. 选择"发送到" → "压缩(zipped)文件夹"
4. 得到 `style_change_agent.zip`

### 方法二：PowerShell 打包

```powershell
# 进入项目上一级目录
cd C:\Users\aodon\Desktop

# 创建压缩包
Compress-Archive -Path "style_change_agent" -DestinationPath "文章风格改写系统.zip" -Force
```

### 方法三：7-Zip 打包（推荐）

```powershell
# 使用 7-Zip（需先安装）
7z a -tzip "文章风格改写系统.zip" "style_change_agent" -mx=9
```

---

## 打包后的文件结构

最终分发的压缩包内容：

```
文章风格改写系统.zip
└── style_change_agent/
    ├── web_app/
    ├── large_text_processor/
    ├── templates/
    ├── style_extractor.py
    ├── style_rewriter.py
    ├── requirements.txt
    ├── requirements_ocr.txt
    ├── 使用说明.md
    └── 启动服务.bat
```

---

## 分发说明

将压缩包发送给他人后，对方需要：

1. **解压到任意目录**（路径不要有中文）

2. **阅读使用说明.md**，按步骤安装：
   - 安装 Anaconda
   - 创建 Python 环境
   - 安装依赖

3. **双击启动服务.bat** 或按说明手动启动

4. **浏览器访问** http://localhost:5000

---

## 注意事项

1. **API Key 不会打包**：用户需要自己申请 DeepSeek API Key

2. **模型配置保留**：model_config.json 会保留默认配置

3. **缓存不打包**：cache/ 和 output/ 目录不打包，用户使用时会自动创建

4. **Python 环境需用户自行创建**：启动脚本会自动检测并提示

---

## 快速打包命令

在 PowerShell 中执行以下命令，一键打包：

```powershell
# 设置项目路径
$projectPath = "C:\Users\aodon\Desktop\style_change_agent"
$parentPath = Split-Path $projectPath -Parent
$projectName = Split-Path $projectPath -Leaf

# 进入项目目录
cd $projectPath

# 创建临时目录
$tempDir = "$parentPath\temp_package"
if (Test-Path $tempDir) { Remove-Item $tempDir -Recurse -Force }
New-Item -ItemType Directory -Path $tempDir | Out-Null

# 复制必要文件
Copy-Item "web_app" "$tempDir\web_app" -Recurse
Copy-Item "large_text_processor" "$tempDir\large_text_processor" -Recurse
Copy-Item "templates" "$tempDir\templates" -Recurse
Copy-Item "style_extractor.py" $tempDir
Copy-Item "style_rewriter.py" $tempDir
Copy-Item "requirements.txt" $tempDir
Copy-Item "requirements_ocr.txt" $tempDir
Copy-Item "使用说明.md" $tempDir
Copy-Item "启动服务.bat" $tempDir

# 创建必要的空目录
New-Item -ItemType Directory -Path "$tempDir\cache" -Force | Out-Null
New-Item -ItemType Directory -Path "$tempDir\output" -Force | Out-Null

# 打包
$zipPath = "$parentPath\文章风格改写系统.zip"
if (Test-Path $zipPath) { Remove-Item $zipPath -Force }
Compress-Archive -Path "$tempDir\*" -DestinationPath $zipPath -CompressionLevel Optimal

# 清理临时目录
Remove-Item $tempDir -Recurse -Force

Write-Host "打包完成: $zipPath" -ForegroundColor Green
```

执行后会在桌面生成 `文章风格改写系统.zip`
