# 文章风格改写系统 - 使用说明

## 项目简介

本系统是一个智能文章风格改写工具，能够：
- 分析源文档的写作风格（词汇、句式、修辞等）
- 按照源文档风格改写目标文档
- 支持超大文件分批处理
- 支持 PDF 和 Word 文档
- 支持扫描版 PDF 的 OCR 识别

---

## 重要说明

**本项目可放置在任意目录运行，路径中支持中文和空格。**

系统会自动识别项目所在目录，无需手动配置路径。

---

## 环境要求

- **操作系统**：Windows 10/11（推荐）
- **Python**：3.9 - 3.11（推荐 3.10）
- **内存**：8GB 以上（OCR 功能建议 16GB）
- **硬盘**：至少 5GB 可用空间

---

## 安装步骤

### 第一步：安装 Anaconda（推荐）

1. 下载 Anaconda：https://www.anaconda.com/download
2. 安装时勾选 "Add Anaconda to PATH"
3. 打开 Anaconda Prompt 或 PowerShell

### 第二步：创建 Python 环境

```powershell
# 创建虚拟环境
conda create -n style_ocr python=3.10 -y

# 激活环境
conda activate style_ocr
```

### 第三步：安装依赖

```powershell
# 进入项目目录
cd 项目解压路径

# 安装基础依赖
pip install Flask Werkzeug python-docx jieba PyPDF2 tqdm requests

# 安装向量检索（可选但推荐）
pip install faiss-cpu

# 安装 OCR 依赖（如需处理扫描版 PDF）
pip install paddlepaddle==2.6.2 paddleocr==2.7.3
pip install pdf2image Pillow numpy

# 安装词云生成（可选）
pip install wordcloud matplotlib
```

### 第四步：安装 Poppler（OCR 功能需要）

OCR 功能需要 Poppler 工具将 PDF 转为图片：

1. 下载 Poppler：https://github.com/oschwartz10612/poppler-windows/releases
2. 解压到 `C:\poppler`
3. 将 `C:\poppler\Library\bin` 添加到系统 PATH 环境变量

---

## 启动方法

### 方法一：命令行启动

```powershell
# 激活环境
conda activate style_ocr

# 进入项目目录
cd 项目解压路径

# 启动 Web 服务
python web_app/app.py
```

启动成功后，浏览器访问：**http://localhost:5000**

### 方法二：一键启动脚本

双击项目目录下的 `start.bat` 文件（需先创建）

---

## 使用流程

### 1. 主页功能

访问 http://localhost:5000 进入主页：
- **风格提取**：上传文档，分析写作风格
- **风格改写**：按照已提取的风格改写新文档
- **超大文件处理**：点击底部链接进入

### 2. 超大文件处理（推荐）

访问 http://localhost:5000/large_text/ 进行完整流程：

**第一步：上传源文档**
- 上传要参考风格的文档（如校史、年鉴）
- 支持 .docx 和 .pdf 格式
- 如果是扫描版 PDF，勾选 "启用 OCR 模式"

**第二步：分析风格**
- 系统自动分批处理文档
- 提取写作风格特征
- 可选择"快速模式"减少 API 消耗

**第三步：编辑风格标签**
- 查看提取的风格特征
- 可手动编辑、添加、删除标签

**第四步：改写目标文档**
- 上传需要改写的文档
- 系统按照源文档风格进行改写

### 3. API Key 配置

使用 DeepSeek 模型需要 API Key：

1. 访问 https://platform.deepseek.com/api_keys 获取
2. 在页面底部的 "DeepSeek API Key" 输入框中填入
3. 系统会自动保存到浏览器

---

## 模型说明

系统支持以下模型：

| 模型 | 速度 | 效果 | 费用 |
|------|------|------|------|
| DeepSeek-V4-Flash | 快 | 好 | 0.001元/千token |
| DeepSeek-V4-Pro | 较快 | 更好 | 0.002元/千token |

推荐使用 **DeepSeek-V4-Flash**，性价比最高。

---

## 快速模式说明

对于风格统一的文档（如校史、年鉴），可使用快速模式：

- **10%**：最快，分析约 1/10 片段
- **20%**：推荐，分析约 1/5 片段
- **30%**：更准确，分析约 3/10 片段

快速模式可减少 70%-90% 的 API 消耗，效果依然可靠。

---

## 常见问题

### Q1: 启动报错 "ModuleNotFoundError"

```powershell
# 确保已激活环境
conda activate style_ocr

# 重新安装依赖
pip install Flask python-docx jieba PyPDF2 requests
```

### Q2: OCR 报错 "poppler not found"

安装 Poppler 并添加到 PATH 环境变量（见安装步骤第四步）

### Q3: 上传 PDF 后无反应

检查文件大小是否超过 100MB，大文件建议使用超大文件处理功能

### Q4: 改写结果不理想

- 尝试使用 DeepSeek-V4-Pro 模型
- 检查源文档风格是否足够典型
- 手动编辑风格标签，添加更明确的指导

### Q5: API Key 无效

- 检查 Key 是否正确复制（无多余空格）
- 检查 DeepSeek 账户余额是否充足
- 尝试重新生成 API Key

---

## 目录结构

```
style_change_agent/
├── web_app/                  # Web 应用
│   ├── app.py               # 主程序入口
│   ├── large_text_api.py    # 超大文件处理 API
│   └── model_config.json    # 模型配置
├── large_text_processor/     # 文档处理模块
│   ├── document_processor.py # 文档切分
│   ├── style_index.py       # 风格索引
│   └── rewrite_agent.py     # 改写代理
├── templates/               # 网页模板
├── cache/                   # 缓存目录
├── output/                  # 输出目录
├── style_extractor.py       # 风格提取核心
├── style_rewriter.py        # 风格改写核心
└── requirements.txt         # 依赖清单
```

---

## 技术支持

如遇问题，请检查：
1. Python 版本是否为 3.9-3.11
2. 所有依赖是否正确安装
3. API Key 是否有效
4. 文件格式是否支持

---

## 版本信息

- 版本：1.0.0
- 更新日期：2025年5月
- 主要功能：风格提取、风格改写、超大文件处理、OCR 支持
